# Gradle
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=false

# Parchment mappings (community parameter names + javadoc for 1.21.1)
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17

# Environment versions (must agree with each other)
minecraft_version=1.21.1
minecraft_version_range=[1.21.1]
neo_version=21.1.250
loader_version_range=[1,)

# Simple Voice Chat API (compile-only, optional at runtime)
voicechat_api_version=2.6.0

# Mod properties
mod_id=jarvis
mod_name=Jarvis
mod_license=MIT
mod_version=1.0.0
mod_group_id=com.jarvis
mod_authors=Jarvis Project
